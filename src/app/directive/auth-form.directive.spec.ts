import { AuthFormDirective } from './auth-form.directive';

describe('AuthFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthFormDirective();
    expect(directive).toBeTruthy();
  });
});
